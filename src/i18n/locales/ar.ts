/**
 * Arabic translations — enable in `src/i18n/provider.tsx` when ready.
 * Copy structure from `en.ts` and translate values.
 */
import type { Messages } from "./en";

export const ar = {} as Messages;
